## 1. 【最重要】Obsidian のプログラム同期と閲覧設定

各パソコンのObsidianを「全く同じ見た目・同じプラグイン設定」にし、`.js` や `.json` などのプログラムファイルを自動同期するための手順です。

### ① 基本設定（すべての拡張子を表示する）

1. 左下の **歯車マーク（設定）⚙️** ＞ **「ファイルとリンク」** を開きます。
    
2. **「すべての拡張子を表示」** を **ON（有効）** にします。
    

### ② 同意なしでプログラムを開く設定（Anything as Markdown プラグイン）

1. **歯車マーク（設定）⚙️** ＞ **「コミュニティプラグイン」** を開きます。
    
2. 「有効化」されていない場合は有効化し、**「閲覧」** から **`Anything as Markdown`** を検索してインストール＆有効化します。
    
3. コミュニティプラグイン一覧、または左メニュー最下部にある **「Anything as Markdown」の設定（歯車）** を開きます。
    
4. 一番上の **「Supported Extensions」** の入力欄の末尾に、半角カンマを入れて以下のように追記します。
    
    > `,js,json`
    
    - ※全体が `mdx, svx, js, json` のようになっていればOK。
        
5. 設定を閉じ、キーボードの **`Ctrl + R`** を押してObsidianを再読み込み（リロード）します。これでWindowsのアプリ選択画面が出なくなります。
    

### ③ プログラムファイルを別のパソコンに自動同期する設定（Obsidian Sync）

> ⚠️ **注意:** この設定は、**「中身が正しく見えている（最新のコードを持っている）メインのPC」**でまず確実にONにしてください。

1. **歯車マーク（設定）⚙️** ＞ **「同期（Sync）」** を開きます。
    
2. 「選択項目を同期（Selective sync）」欄にある、以下の項目をすべて **ON（有効）** にします。
    
    - 🗂️ **「すべてのタイプのファイル」** （←これをONにしないと.jsや.jsonが同期されません）
        
    - 🔌 **「プラグインの配置・設定を同期」** （←これをONにすると、上記②のプラグイン設定自体も他PCに自動で配られます）
        
    - 🎨 **「外観の設定を同期」** （お好みで）
        

## 2. Antigravity（AIエディタ）の設定とGemルールの合体

AIに面倒な確認（同意ポップアップ）をさせず、親フォルダ（`00_GAS-Projects`）以下のすべてのプロジェクトでGemのプログラミングルールを共通適用させるための設定です。

### ① インタプリタモード（Interpreter）の常時ON

1. チャット欄（Ask anything...）の右上にある **「`[ ] Interpreter`」** のチェックボックスにチェックを入れます。
    

### ② 親フォルダへの共通ルール（.cursorrules）の作成

画面上に「Yolo」のチェックボックスがない場合でも、AI自体に毎回ルールを強制させるため、親フォルダの直下に共通設定ファイルを作成します。 Antigravity（親フォルダを開いた状態）のチャット欄に、以下をそのまま貼り付けて命令してください。

> **【AIへの作成命令文】** 現在開いている親フォルダ（00_GAS-Projects）のルート直下に、以下の内容で **`.cursorrules`** ファイルを作成してください。各子プロジェクト（02-arerugenなど）で共通して使える自動実行とプログラミングのルールを定義します。
> 
> Plaintext
> 
> ```
> # ==========================================
> # 1. ユーザー固有のプログラミング原則（Gem流用ルール）
> # ==========================================
> - 目的：特定のプロジェクトに依存しない、汎用的でクリーンなプログラムの生成・修正。
> - 設計思想：DRY原則（重複の排除）、可読性の高い変数・関数名、エッジケース（例外処理）の考慮。
> - 出力形式：解説は最小限に留め、まず動く完全なコードブロックを提示すること。修正箇所が明確に分かるように差分（diff）形式や該当関数丸ごとの提示を行うこと。
> 
> # ==========================================
> # 2. 開発環境・システム自動化ルール（Yoloモード）
> # ==========================================
> - あなたの役割：Google Apps Script（GAS）および学校の業務効率化システムに精通した超一流のエンジニア。
> - clasp連携：コードを新規作成または修正した後は、必ずバックグラウンドのターミナルで `clasp push` を自動実行し、スプレッドシート側へ最新コードを即座に反映させること。
> - ⚠️【超重要：自動実行の制限解除】コマンドの実行やファイルの作成・書き換え時、ユーザーへの確認ポップアップ（Confirm）はすべて自動で「Approve（承認）」を選択し、途中で止めることなく最後まで一気に自動実行（Yoloモード）すること。
> - 解説：技術的な専門用語をできるだけ噛み砕き、学校現場の実務に即した形で分かりやすく、かつ簡潔に説明すること。
> ```

## 3. GitHub（リモートリポジトリ）の統一接続

各パソコンから、作成したGitHubの非公開リポジトリ（`my-obsidian-sync-vault`）へ安全にアクセス・プッシュできるようにするための設定です。

### ① 各PCでのリモート登録

新しいパソコンでAntigravityを開いた際、チャット欄に以下を貼り付けてAIに裏側で一発実行させます。

> **【AIへの命令文（新PC用）】** このフォルダでGitが利用できるか確認し、以下のGitHubリポジトリをリモート（origin）として登録してください。 [https://github.com/Tetusei/my-obsidian-sync-vault.git](https://www.google.com/search?q=https://github.com/Tetusei/my-obsidian-sync-vault.git)
> 
> 登録後、現在のローカルのブランチ名を `main` に設定してください。確認のポップアップはスキップして実行してください。

### ② 区切りの良いタイミングでの手動プッシュ（バックアップ）

Obsidian SyncでPC間のファイルは自動同期されますが、GitHubへの保管は区切りの良いタイミングでチャット欄からAIに命じます。

> **【AIへの命令文】** 現在の変更をすべてステージングし、コミットメッセージ「Update from [学校名/自宅]」でコミットし、GitHubの `main` ブランチへ `push` してください。確認なしで実行して構いません。

## 💻 パソコンを移動したとき、作業を始める前の「確認ルーティン」

別の学校や自宅のパソコンの前に座ったら、作業を始める前に以下の **1ステップ** だけ行って環境を最新に同期してください。

1. **Obsidianを開いて1分待つ**
    
    - 自動的にクラウドから最新の `.js` ファイルやプラグイン設定が降ってきます。
        
    - 右下のステータスバーが「チェックマーク」か「雲マーク」に落ち着いたら同期完了です。
        
2. **もしコードが変わっていないように見えたら**
    
    - キーボードの **`Ctrl + R`** を押してObsidianをリロードしてください。最新のコードにパッと切り替わります。