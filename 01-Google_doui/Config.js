/**
 * @file値 Config.gs
 * @description システム全体の定数・設定情報およびバージョンを管理します。
 * @version v1.1.0
 */

const Config = {
  // ────────────────────────────────────────────────────────
  // 1. システムバージョン管理（唯一の正本）
  // ────────────────────────────────────────────────────────
  VERSION: 'v1.1.0',

  // ────────────────────────────────────────────────────────
  // 2. 使用するシート名の設定
  // ────────────────────────────────────────────────────────
  SHEET_NAMES: {
    BASE: '基礎データ',
    LIST: '一覧',
    FOLDER: 'フォルダ',
    SPLIT: 'PDF分割'
  },

  // ────────────────────────────────────────────────────────
  // 3. 各シート内のセル・列位置の設定（データレイアウト）
  // ────────────────────────────────────────────────────────
  // 「基礎データ」シート
  BASE_CELL_FOLDER_URL: 'B2', // 同意書フォルダのURL/IDが入るセル

  // 「一覧」シート（1始まりの列番号）
  LIST_COL_NAME: 6,      // F列: 氏名
  LIST_COL_EMAIL: 10,    // J列: メールアドレス
  LIST_COL_FILENAME: 11, // K列: 自動生成ファイル名の出力先
  LIST_COL_URL: 12,      // L列: マッチしたPDFのURL出力先

  // 「フォルダ」シート（1始まりの列番号）
  FOLDER_COL_NO: 1,      // A列: 連番
  FOLDER_COL_NAME: 2,    // B列: ドライブ内のファイル名
  FOLDER_COL_URL: 3,     // C列: ドライブ内のファイルURL
  FOLDER_COL_CHECK: 4,   // D列: 照合チェック結果（〇 / ✖）

  // 「PDF分割」シート（1始まりの列番号およびセル位置）
  SPLIT_CELL_DEST_URL: 'H2', // ★修正: 移動先フォルダのURL/IDが入る共通セル
  SPLIT_COL_STATUS: 7,       // ★修正: 移動完了ステータスの書き込み先（G列）
  
  // ────────────────────────────────────────────────────────
  // 4. バックアップ設定
  // ────────────────────────────────────────────────────────
  BACKUP_FOLDER_NAME: '📦_バックアップ保存箱'
};